version: 2.1
orbs:
  cypress: cypress-io/cypress@1
jobs:
  deploy:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
workflows:
  ex:
    jobs:
      - cypress/run:
          yarn: true
      - deploy