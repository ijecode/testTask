version: 2.1
orbs:
  cypress: cypress-io/cypress@1
jobs:
  deploy:
    machine:
      - enabled: true
    steps:
      - run:
          name: SSH Deployment to DigitalOcean
          command: |
            echo 'Hello World!'
workflows:
  ex:
    jobs:
      - cypress/run:
          yarn: true
      - deploy