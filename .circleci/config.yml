version: 2.1
orbs:
  cypress: cypress-io/cypress@1
jobs:
  deploy:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
workflows:
  ex:
    jobs:
      - cypress/run:
          yarn: true
      - deploy:
          requires:
            - cypress/run